import React from 'react';
import Slider from './Slider/Slider';

const Home = () => {
    return (
        <section className='container mt-5'>
            <Slider></Slider>
        </section>
    );
};

export default Home;